# Workspace environment files are loaded per application.
# Copy server/.env.example to server/.env and client/.env.example to client/.env.
# Generate independent random JWT secrets; never commit actual values.
